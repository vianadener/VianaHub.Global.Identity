Vou considerar duas hipóteses objetivas: ou eu estava vendo uma versão/cache diferente do GitHub, ou o caminho/branch validado não era exatamente o mesmo que o Codex local validou. Vou checar o remoto público de novo pelos arquivos brutos, que é mais confiável que a navegação visual.

Pensou por 6s

Você está certo em desconfiar: pelo raw público da branch develop, o po/SKILL.md ainda aparece sem frontmatter YAML. Ele começa direto com # Agente PO, sem:

---
name: po
description: ...
---

Fonte pública atual:

Então há uma divergência real entre o que o Codex local está dizendo e o que o GitHub público está servindo via raw.githubusercontent.com.

Possíveis causas:

O Codex está validando outro repositório/local clone.
O remote local não é vianadener/VianaHub.Global.Identity.
A branch local develop não corresponde ao develop público que estou acessando.
O arquivo foi corrigido localmente em outro caminho, mas não em .agents/skills/po/SKILL.md.

Rode estes comandos no terminal:

git remote -v
git rev-parse HEAD
git ls-tree -r HEAD .agents/skills/po/SKILL.md
type .agents\skills\po\SKILL.md

O último comando precisa mostrar o frontmatter logo no início.